[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "minchoagnt"
version = "0.1.0"
description = "A tiny Hermes-style memory and skill loop."
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
minchoagnt = "minchoagnt.cli:main"
